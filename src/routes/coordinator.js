export const goToHomePage = (navigate) => {
    navigate('/')
}

export const goToInfosPage = (navigate, id) => {
    navigate(`/infos/${id}`)
}

export const goToBuyPage = (navigate) => {
    navigate("/buypage")
}